// Instalación de Angular
npm install -g @angular/cli
// Creación de proyecto
ng new nombre-del-proyecto
// Iniciar proyecto
npm start ó ng serve
// Generación de componentes
ng generate component usuario    -      ng g c usuario
// Versionamiento en GIT
git init
git remote add origin URL-del-repositorio
git add .
git commit -m "Proyecto Angular inicial"
git push -u origin main

// Proyecto de Clase
1. npm i bootstrap@5.3.7
2. Angular.json/styles "node_modules/bootstrap/dist/css/bootstrap.min.css"
3. ng g c components/navbar
4. ng g c components/agregarEditarComentario
5. ng g c components/listComentarios 
6. app.html add listComentarios and integrate with ts file in imports.
7. ng g i interfaces/comentario 
8. ng g s services/comentario
9. add navbar of bootstrap in the component navbar.
10. add code for listComentarios.html and add container in app.componets for main app
11. add button "Nuevo comentario" in listComentarios.
12. add table in listComentarios with class table table-hover mt-3, with 5 columns
13. add var listComentarios in componet ts listComentarios of type comentario interface.
14. use *ngFor=let comentario of  and user interpolation {{  }}
15. add conditions @if (listComentarios.length === 0)
16. Add routes in the app.routes.ts and edit app.cofig.ts
17. Add routerLink in button add new comment in listComentarios.
18. Import RouterLink in ts for listComentarios.
19. Add HTML for Comentario.

1. Installar Docker en su computadora.
2. Dockerizar el proyecto actual de angular.
    a. Crear un archivo docker-compose.yml, (Crear una cuenta en docker hub.)
    b. Crear presentación
    c. Explicar a detalle el funcionamiento del archivo docker-compose.yml
    d. Levantar el proyecto con docker.
3. VPS (Linux -> Ubuntu) y acceder al VPS  por SSH.